#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define N 30
#define MAX 100
int main() {
	srand(time(NULL));
	int A[N];
	for (int i = 0; i<N; i++){
		A[i] = rand()%(MAX+1);
	}
	
	for (int i = 0 ; i<N; i++){
		printf(" %d\n", A[i]);
	}
	return 0;
}
